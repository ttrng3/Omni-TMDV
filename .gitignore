# Never commit credentials. The weekly job writes through the GitHub MCP file
# tools with an injected credential and reads no token from this tree.
# The runbook is README.md — there is deliberately no second copy.
.github-token
.github-token.save
*.save
*.token

# Local Cowork working files — this checkout doubles as the Cowork artifact
# folder, which keeps its own scratch alongside the repo.
.DS_Store
thumbnail.png
versions/
debug.jsonl
CHANGELOG.md
SKILL.md.proposed
sitecheck-history.json
sitecheck-refresh-*.md
build/
